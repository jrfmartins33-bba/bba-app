/**
 * Carimbo manual de versão do Decision Engine (Sprint 13.8 —
 * `decision_snapshots.engine_version`). Incrementar sempre que uma
 * regra, rule pack ou cálculo que afete `Decision`/`Recommendation`
 * mudar de verdade — não é semver automático, só um carimbo estável
 * para saber com qual versão da lógica um snapshot foi calculado.
 */
export const ENGINE_VERSION = "2026.07.1";
